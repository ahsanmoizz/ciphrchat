services:
  relay:
    build: .
    ports:
      - "4001:4001/tcp"
      - "4001:4001/udp"
      - "8080:8080/tcp"
    volumes:
      - relay-data:/var/lib/ciphrchat
    restart: unless-stopped

volumes:
  relay-data:
